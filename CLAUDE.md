# משחקי דו-סול-פה

שני משחקים מתוך "המשפחה של סול" — פלטפורמה מוזיקלית לילדים בגילי 4-8.

- https://hanibarsol.github.io/dosolfa-games/finddifferences1.html — עולם 8, שיעור 5
- https://hanibarsol.github.io/dosolfa-games/finddifferences2.html — עולם 8, שיעור 6

## שני קבצים כאן הם הפניות, לא משחקים

`dosolfa.html` ו-`rhythm.html` היו עותקים שניים של משחקים שכבר חיים
במאגרים משלהם. עותק שני פירושו ששינוי נוחת באחד ולא בשני, ולכן ב-2026-08-19
הוחלף התוכן שלהם בהפניה אוטומטית:

| הקובץ | מפנה אל |
|-------|---------|
| `dosolfa.html` | https://hanibarsol.github.io/makhelet-do-re-mi/ |
| `rhythm.html` | https://hanibarsol.github.io/sol-rhythm-basic/ |

**אין להחזיר לתוכם משחק ואין לערוך אותם כשיעורים.** שינוי במקהלת נעשה
ב-`makhelet-do-re-mi`, ושינוי במשחק הקצב הבסיסי ב-`sol-rhythm-basic`.

שני השיעורים החיים בתיקייה הזו הם `finddifferences1.html` ו-`finddifferences2.html`
בלבד.

## איך מעלים שינוי — חשוב

**רק דרך git.** `git pull` לפני כל עריכה, ואז commit ו-push רגילים.

אין להעלות דרך סקריפט עם טוקן ל-API של גיטהאב. שיטה כזו דורסת בשקט
עבודה של שיחות אחרות — זה קרה בפלטפורמה הזו חמש פעמים ומחק את כפתור
הבית בכל פעם. git מסרב לדרוס, וזו בדיוק ההגנה שרוצים כאן.

אם git מסרב לדחוף — זו לא תקלה. מישהו נגע בקובץ בינתיים. לעשות pull,
לבדוק מה השתנה, ולשלב במקום להחליף.

## כפתור הבית — לא לשנות

עיגול טורקיז `#17A79B` עם בית לבן, פינה שמאלית עליונה, 52px
(44px מתחת ל-640px רוחב). מוביל לפורטל Smoove עם `target="_top"`.

זהו הכפתור האחיד בכל 27 הדפים של הפלטפורמה, מ-2026-08-16. הוא קיים
בשני המשחקים כאן. אם משחזרים גרסה ישנה של קובץ — לוודא שהכפתור שרד.

## כללי פינות

- **שמאל למעלה** שמור לכפתור הבית בלבד
- שום כותרת או כפתור לא יושב מתחת לכפתור הבית. אם צר מדי במסך טלפון —
  להוסיף `padding-left` לשורה, **לא** להקטין את התוכן
- בשני משחקי ההבדלים כבר קיים `padding-left` ל-`.hero` מתחת ל-640px
  רוחב, אחרי שנמדד שהכפתור מכסה את הכותרת בטלפון. לא להסיר.

## דברים שנראים מיותרים ואינם

- מסך הניצחון יושב ב-`z-index` 50, וכפתור הבית ב-40 — בכוונה, כדי
  שהכפתור לא ירחף מעל מסך הניצחון. לא להעלות את המספר של הכפתור.

## הטבעת שמסמנת הבדל שנמצא — 2026-08-25

`finddifferences1.html` בלבד. הני דיווחה שהטבעת שליד העציץ נראית חלקית.

### הסיבה — לא צפיפות, אלא מילה אחת

הטבעת ממוקמת ב-`left`, ומורכזה עם `margin-inline-start`. הדף הוא
`dir="rtl"`, ושם `inline-start` הוא **הצד הימני** — ושוליים ימניים אינם
מזיזים תיבה שממוקמת ב-`left`. כלומר **המרכוז מעולם לא פעל**, וכל תשע
הטבעות צוירו חצי טבעת ימינה ממה שהן מסמנות.

לכן הטבעת של הקולר נחתה על התווים שליד החתול, זו של החגורה על הקלידים,
וזו שליד הקצה הימני גלשה מהתמונה ונחתכה.

נמדד: לפני — 18 עד 31 פיקסלים מההבדל, ובאחד המקרים ההבדל היה מחוץ
לטבעת. אחרי — 0 עד 10.

**התיקון: `margin-left` במקום `margin-inline-start`. אין להחזיר.**
`margin-top` היה תקין מלכתחילה ולכן הסטייה הייתה אופקית בלבד.

### הגודל נמדד ביחס לתמונה, לא ב-rem

הטבעת הייתה 2.6rem קבועים. התמונה מתכווצת עם המסך והטבעת לא, ולכן
בטלפון היא תפחה לכחמישית מרוחב התמונה ולא יכלה להיכנס ליד הקצה
**בשום מיקום**. כעת `width: 10%` עם `aspect-ratio: 1`, והשוליים
`-5%`. שוליים באחוזים נמדדים תמיד מול **רוחב** המכל — גם `margin-top` —
ולכן שני החצאים תואמים. צריך גם `box-sizing: border-box`, אחרת המסגרת
של 3 פיקסלים מוסיפה לרוחב והטבעת שוב חורגת.

הסימן שבתוכה עבר ל-`55%` כדי להתכווץ יחד איתה.

### שתי נקודות שהוזזו ביד

| מפתח | בקוד | ההבדל האמיתי | למה |
|------|------|--------------|-----|
| `door` | 0.8152 | 0.8152 | הותאם למרכז האמיתי |
| `leaf` | 0.9400 | 0.9622 | שמאלה בכוונה |

`leaf` יושב ליד הקצה הימני. גם אחרי המרכוז, טבעת שלמה לא נכנסת שם אם
היא ממורכזת על העלה עצמו. **ההזזה שמאלה מכוונת** — העלה נשאר בתוך
הטבעת ובתוך אזור הלחיצה (`fr: 0.040`, כלומר 0.900 עד 0.980).
אין "לתקן" את זה חזרה ל-0.9549.

### איך מוצאים מרכז אמיתי של הבדל

לא בעין. שתי התמונות הן base64 באותו קובץ; מציירים כל אחת לקנבס,
משווים פיקסלים עם סף (הפרש RGB מעל 90, כי JPEG מרעיש), ומקבצים לפי
קרבה. תשעת האשכולות הגדולים הם תשעת ההבדלים. כך נמצאו `door` ו-`leaf`.

### אותו באג קיים בשלושה מקומות נוספים — לא נגענו

`.ripple`, `.hint-ring` ו-`.lens` משתמשים גם הם ב-`margin-inline-start`
ולכן מוסטים ימינה. `.ripple` הוא אפקט חולף של כחצי שנייה ולכן הושאר.
**אין לגעת ב-`.lens` בלי בדיקה** — זכוכית המגדלת נחתכת בכוונה על ידי
`overflow: hidden` של `.canvas`, וזה מה שמונע ממנה לגלוש מהתמונה.

### מה נבדק לפני העלאה

חמישה רוחבי מסך — 320, 390, 500, 820, 1100: אף טבעת לא נחתכת, ואף
שתיים לא נצמדות (מרווח מזערי 4 פיקסלים ב-320). המשחק שוחק עד הסוף
בשני רוחבים: 9 מתוך 9 נתפסים ומסך "כל הכבוד" מופיע.

**לא נגעו בתמונות, בצלילים, ברמזים, בזכוכית המגדלת, בכפתור הבית
ובחצי המעבר.**

## עקרונות תוכן

- בלי מוזיקת רקע מתמשכת — רק צלילים קצרים
- כשמסך לא נכנס: לקצץ שוליים וניווט, אף פעם לא את תוכן המשחק
- הטקסט והקצב פונים לילדים בני 4-8
- לבדוק כל שינוי גם במסך טלפון, לא רק במחשב

## שיטת עבודה

שיחה אחת לפרויקט, ולא שתי שיחות על אותו קובץ במקביל.
לפני שמדווחים שמשהו תוקן — לבדוק שזה באמת עלה לאתר החי.

## הניווט בין השיעורים — 26.8.2026

### אין להחזיר כתובות של סמוב לתוך השיעורים

הכתובות שהיו כאן — בכפתור הבית ובחצים — הכילו מזהה פורטל **ששייך
לחשבון שממנו הועתקו.** סמוב מנפיקה מזהה שונה לכל חשבון, ולכן הן עבדו
אצל הני ושלחו **כל לקוח אחר למסך התחברות באמצע השיעור.**

חברה של הני הוכיחה את זה ב-26.8: עם החשבון של הני הכל עבד, עם החשבון
שלה נשבר. אצל הני זה תמיד ייראה תקין — הכתובות הן שלה.

**כל כתובת `portal.smoove.io` שנכתבת לכאן שבורה מראש לכל לקוח.**

### כפתור הבית

מחזיר לעמוד שממנו הילד הגיע (`history.back`), בלי שום מזהה — כך הוא
נוחת בפורטל **שלו**, על העמוד שעזב. הכתובת הישנה נשארת בקוד **כגיבוי
בלבד**, למקרה שהשיעור נפתח בלשונית חדשה ואין לאן לחזור.

הסקריפט מסומן `sol-home-back`. **אין להחליף אותו בכתובת קבועה.**

### מקומו של השיעור בשרשרת

רצף אחד רציף לאורך שמונת העולמות. **המאגר הזה מחזיק שתי תחנות:**

**`finddifferences1.html` — תחנה 33 מתוך 34, עולם 8**

| | |
|---|---|
| **אחורה** | חדר המשחקים  (`room-puzzle`) |
| **קדימה** | מצאו 6 הבדלים  (`finddifferences2.html`) |

**`finddifferences2.html` — תחנה 34, האחרונה בקורס**

| | |
|---|---|
| **אחורה** | מצאו 9 הבדלים  (`finddifferences1.html`) |
| **קדימה** | **אין** — זו התחנה האחרונה |


החצים מפנים **ישירות לשיעור השכן** ולא דרך סמוב. אם משנים את סדר
הקורס — לעדכן את שני השכנים, לא רק את השיעור הזה.
